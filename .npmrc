# npm default, pinned explicitly: some machines have `bin-links=false` set
# globally, which silently skips node_modules/.bin and breaks `prisma`,
# `next` and `tsx` in the npm scripts.
bin-links=true
audit=false
fund=false
